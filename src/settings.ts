const config: any = {
  ensureAuth: true, //Set to false to allow unauthenticated requests to all routes
  graphApiBaseUrl: "https://graph.facebook.com/v18.0/",
  serverBaseUrl:"http://69.49.247.218:8457"
};

export default config;
